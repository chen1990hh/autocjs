div#option-article.api-md
    div.api-hd
        h3.api-title article
    div.api-bd
        h4 Description
        dl.api-param
            dt.api-param-key Type：
            dd.api-param-value String | HTMLElement
        dl.api-param
            dt.api-param-key Default：
            dd.api-param-value ''
        p 必选，用来指定页面中显示文章正文的 DOM 节点或者 ID 选择器。如果没有指定它，则程序将不会执行。
        h4 Usage
        h5 As a id selector
        pre.api-code
            code.
                new AutocJS({
                    article: '#article'
                    // ...
                });
        h5 As a DOM element
        pre.api-code
            code.
                new AutocJS({
                    article: $('#article')
                    // ...
                });