div#method-toggle.api-md
    div.api-hd
        h3.api-title toggle()
    div.api-bd
        h4 Description
        p toggle() 方法用来展开侧或者收起边栏菜单。
        h4 Returns
        dl.api-param
            dt.api-param-key Type：
            dd.api-param-value AutocJS
        p AutocJS 对象。
        h4 Usage
        pre.api-code
            code.
                var ArticleTOC = new AutocJS({
                     article: '#article',
                     title: 'AutocJS v0.2.0'
                });

                ArticleTOC.toggle(); // -> 导航菜单是收起的，菜单将展开。导航菜单是展开的，菜单将收起。