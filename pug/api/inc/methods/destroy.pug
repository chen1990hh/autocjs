div#method-chapters.api-md
    div.api-hd
        h3.api-title destroy()
    div.api-bd
        h4 Description
        p destroy() 方法用于移除所有绘制的 DOM 节点，并移除绑定的事件处理器。
        h4 Returns
        dl.api-param
            dt.api-param-key Type：
            dd.api-param-value {AutocJS}
        p AutocJS 对象。
        h4 Usage
        pre.api-code
            code.
                var ArticleTOC = new AutocJS({
                     article: '#article',
                     title: 'AutocJS v0.2.0'
                });

                ArticleTOC.destroy(); // -> 将在页面中移除 AutocJS 导航菜单